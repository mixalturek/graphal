/////////////////////////////////////////////////////////////////////////////
//// Code clean up

add error and warning messages if ValueNull is returned
resolve all TODOs
ValueIdentifier and ValueReference double dispatching cleanup!!!

add position to the generated code
	#line number "gen_operators.pl"
	#line number "gen_builtin.pl"


/////////////////////////////////////////////////////////////////////////////
//// Built-in functions

type casting
	toInt()
	toBool()
	...
type checking
	isInt()
	isGraph()
	isIterable()
	...
graphs
breakpoint(condition)
	add command line option --breakpoints to enable/disable


/////////////////////////////////////////////////////////////////////////////
//// Tests

. [] operators
	check member access to edges and vertices (ValueStruct had bugs)
test multiple assignment
	a = array[3] = b = 2;
unit tests
	add new for basic things
	split to several files by groups (values, nodes, builtins ...)
	resolve ?gcc bug? - Tests::testValueArray()
	why is cout << ""; needed on windows
		ValueGraph::deleteVertex()
		Tests::testGraph()


/////////////////////////////////////////////////////////////////////////////
//// Bison

rewrite parser to C++ (it is in documentation)
function overloading (save names like "name<numberofparameters>")
	array() - array0()
	array(size) - array1()
add foreach, iterators
add "global variable;" statement
add switch
