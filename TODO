different shapes of vertices
built-in function to paint special objects to the scene
settings dialog, add new items
save: remove spaces at the end of lines

resolve all TODOs
create bbd library
resolve ?gcc bug? - Tests::testValueArray()
QFileDialog - default path, file types, save directory to the settings

Bison
	rewrite parser to C++ (it is in documentation)
	function overloading (save names like "name<numberofparameters>")
		array() - array0()
		array(size) - array1()
	add switch

write doxygen documentation
write language documentation
