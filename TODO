different shapes of vertices
built-in function to paint special objects to the scene

resolve ?gcc bug? - Tests::testValueArray()

Bison
	rewrite parser to C++ (it is in documentation)
	function overloading (save names like "name<numberofparameters>")
		array() - array0()
		array(size) - array1()
	add switch


Licensing
	libgraphal LGPL, fix file headers
	other GPL

Simple ./configure script.
Global Makefile.
make install
	test
	headers

Replace all occurrences of "bbd" by "graphal".
Replace links to http://woq.nipax.cz/.
MainWindow::about().
Extend sloccount target.
TODOs and FIXMEs in Makefiles.
Web.
